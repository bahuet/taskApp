
general: 
- fix userview
- explication des actions possibles quand on arrive sur la page (tutoriel) ? 
- footer avec import ContactMailIcon from '@material-ui/icons/ContactMail';

styling / CSS:
- rendre adminview responsive, et régler les gap entre élements (flexbox)
- intégrer le a:chosen du stylesheet dans l'app
- tooltips ? 
- transitions sur les Fab avec <Zoom>

admin:
- alerte si un utilisateur n'as pas de taches actives
- differents ordres d'affichage
- supprimer toutes les taches completed
- renommer user
- transfer de taches inter users avec autocomplete

user:
- alerte si tache urgente non focus 
- une seule tache peut etre focus ?
- user avatar
- les users ont un nom + un rôle + id
- task => title + description instead of just text?

plus tard peut-être:
- possibilité de classer les users par groupe avec background de couleur?
- fake loading times
- tableau statistique pour l'admin dashboard
- historique des taches faites avec le bouton expand
